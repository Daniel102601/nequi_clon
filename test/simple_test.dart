import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Prueba de humo para verificar el Pipeline', () {
    // Esta prueba siempre pasa. Es solo para que Flutter no se queje.
    expect(true, true);
  });
}